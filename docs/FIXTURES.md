# Parity fixtures

Every `.docx` under `apps/demo/public/fixtures/` used for Word-parity work,
what feature cluster it exercises, its Word page count, and how to regenerate
it. Fixtures and their Word reference PDFs are **gitignored** — the committed
deliverable is the generator scripts listed here. Regenerate a fixture, export
its Word reference once, then run the pixel harness.

## How the bed works (recap)

- Fixtures: `apps/demo/public/fixtures/<name>.docx` (regenerated by scripts).
- Word ground truth: `parity/<name>-word.pdf` (exported once via local
  Microsoft Word; see "Exporting" below).
- Pixel comparison: `node scripts/parity-compare.mjs <names...> --base http://localhost:5299`
  writes `parity/out/<name>-p<N>.png` (Word | web | diff) and prints a
  per-page **structural %** and **raw %**. The dev server must be running
  (`npm run dev`, default port 5299).

The two numbers differ: **raw %** is the fraction of page pixels that differ
(stable, but dilutes on sparse pages and inflates on dense ones). **structural
%** blocks the page and aligns before scoring, so it is far more sensitive to
sub-line *vertical* offsets — a page whose content matches but sits one line
lower can score 90%+ structural at ~5% raw. Read them together: high structural
+ low raw = alignment drift; high both = real missing/wrong content.

## Regenerating

```
# hand-built raw-XML fixtures (parity-* batch 1)
python3 scripts/make-parity2-fixtures.py          # revisions, math, math2, columns,
                                                   #   colbalance, pageborders, rowsplit
python3 scripts/make-hftemplate-fixture.py         # parity-hftemplates

# batch-2 hand-built fixtures (this expansion)
python3 scripts/make-parity2-more.py               # the 10 parity2-* hand-built fixtures

# batch-2 template-derived fixtures (this expansion)
python3 scripts/make-parity2-templates.py          # coverpage, watermark, equations

# ALWAYS validate before exporting to Word (hard gate; see "Validating" below)
python3 scripts/validate-docx.py

# probes (single-feature calibration docs, not in the parity table)
node   scripts/make-advance-probe.mjs
python3 scripts/make-*-probe*.py
```

The older docx-lib fixtures (`parity-tables`, `parity-lists`, `parity-text`,
`parity-firstpage`, `parity-headerfooter`, `parity-comments`, `parity-dividers`,
`parity-pictures`, `parity-wrapmodes`, `parity-math` stubs, `sample`,
`benchmark`, `exact`) were authored with the `docx` npm library; the
sanitized real-document fixtures (`msa`, `chronology`, `pleading`, `forsale`,
`pickett`, `real`) come from `scripts/sanitize-docx.py`.

## Validating BEFORE Word (required gate)

A fixture that Word has to REPAIR is not trustworthy ground truth — Word's
repaired in-memory doc may not match the `.docx` bytes our engine renders, and
a corrupt open can wedge the AppleEvents queue for the rest of an export batch.
So every fixture must pass validation before it goes to Word:

```
python3 scripts/validate-docx.py                   # all parity2-*.docx
python3 scripts/validate-docx.py <name> [<name>]   # specific fixtures
```

It checks zip integrity, XML well-formedness of every part, relationship /
content-type / r:id / footnote-endnote-id / numId cross-references, and — most
importantly — **OOXML child ORDER and duplicate singletons** in the ordered
complex types (`w:pPr`, `w:rPr`, `w:tcPr`, `w:tblPr`, `w:sectPr`, `w:trPr`,
`w:settings`). Out-of-order or duplicated children are the classic raw-XML
cause of Word's "corrupt / repaired" dialog, and both LibreOffice and our own
engine silently tolerate them — so this checker, not an external converter, is
the real Word-corruption detector. `scripts/export-parity2.sh` runs it as a
hard gate and refuses to export a fixture that fails.

A useful independent sanity check (strict enough to catch gross malformation,
lenient about ordering) is LibreOffice: `soffice --headless --convert-to pdf
--outdir /tmp/out -env:UserInstallation=file:///tmp/loprofile <fixtures...>`.

## Exporting Word references

```
scripts/export-parity2.sh <name> [<name> ...]      # this expansion's exporter (validates first)
```

It validates each fixture (hard gate — see above), then opens it in a
**running, unlocked** Microsoft Word via AppleScript `open file name` (the
LaunchServices `open -g` route stops delivering documents after a Word
force-quit), saves as PDF addressing the document **by name**, closes it
`saving no`, then verifies page count + first line. See operational notes at
the bottom.

---

## Batch-2 fixtures (this expansion) — with fresh baselines

Baselines captured at git `HEAD` (main) on 2026-07-08. Format per page:
`structural% / raw%`. "Worst page" is the highest structural score.

| Fixture | Covers | Pages (Word) | Per-page structural% / raw% | Notes |
|---|---|---|---|---|
| parity2-nestedtables | 3-level nested tables; gridSpan + vMerge merges; per-cell single/double/dashed/dotted borders + shading; a vertical (`textDirection=btLr`) cell; a wide gridSpan-header grid | 3 | p1 35.3/10.6 · p2 83.9/5.3 · p3 5.0/3.3 | Structure/shading/borders match; Word rows render slightly **taller**, so text drifts down-page and inflates p2 structural (raw only 5%). |
| parity2-sections | 4 sections mixing portrait/landscape, 1/2/3 columns, and 4 different margins mid-document (nextPage breaks) | 5 | p1 2.3/3.3 · p2 76.3/10.8 · p3 59.7/12.1 · p4 46.6/0.6 · p5 94.0/6.7 | Orientation/column/margin changes all honored; content matches. High structural on the landscape/column pages is **sub-line vertical drift**, not missing content (raw 6-12%). |
| parity2-lists | multilevel decimal/letter/roman/bullet list; legal 1/1.1/1.1.1 scheme; list restart via 2nd num instance; custom colored Wingdings/Symbol bullets | 2 | p1 4.1/3.9 · p2 1.4/4.1 | Clean — lists, numbering, restart, custom bullet fonts all render. |
| parity2-charstyles | highlight; shading; char spacing (expand/condense); kern; caps/smallCaps; strike/dstrike; outline/emboss/imprint; `w:w` scaling; underline varieties; baseline position | 2 | p1 19.1/5.1 · p2 1.7/0.7 | Mostly clean; p1 residual from outline/emboss/imprint 3-D text stroking vs Word. |
| parity2-tabs | left/center/right/decimal/bar tab stops; dot/hyphen/underscore/middleDot/heavy leaders; dot-leader price rows; default tab increments | 2 | p1 5.7/3.7 · p2 70.5/0.2 | Tabs + leaders render. p2 is a **one-paragraph pagination-boundary** offset at a keepNext heading (raw 0.2%). |
| parity2-notes | footnotes AND endnotes, heavy, multi-page, with separators + multi-line note bodies | 2 | p1 50.0/10.6 · p2 17.0/9.2 | Notes render; residual is footnote-area vertical placement + note-body wrapping differences. |
| parity2-fields | SEQ (Figure/Table); STYLEREF; DATE/CREATEDATE; PAGE/NUMPAGES; REF to bookmark — all from cached results | 2 | p1 2.9/4.4 · p2 1.9/5.2 | Clean — cached field results render as text as expected. |
| parity2-dropcap | dropped + in-margin drop caps (`framePr`); a bordered+shaded paragraph crossing a page boundary | 2 | p1 63.1/11.8 · p2 38.8/9.0 | **Drop-cap geometry differs**: the framed big letter's size/hang and the wrap of body text around it don't match Word, shifting the first lines. |
| parity2-textboxes | wps text boxes: square/tight/topAndBottom/behind wrap; a 45°-rotated box; body text flowing around | 2 | p1 72.6/13.4 · p2 94.1/6.6 | Text-box **position + wrap geometry differ**; rotated box placement off. Biggest hand-built parity gap. |
| parity2-toc | a real TOC field (begin/instrText/separate/cached lines/end) with dot-leader tabs + PAGE numbers over Heading1/2/3 spanning several pages | 6 | p1 2.8/1.8 · p2 58.9/12.1 · p3 1.9/6.0 · p4 2.0/5.9 · p5 2.0/5.9 · p6 90.9/4.7 | TOC lines + leaders render (p1 clean). p2/p6 are pagination-boundary offsets (which heading lands where); raw stays 4-12%. |

### Template-derived (this expansion)

Instantiated from real Microsoft Word built-in building blocks so the render
path meets producer-quirk XML. Source: `/Applications/Microsoft Word.app/Contents/Resources/en.lproj/Document Elements/*.dotx`.

| Fixture | Source building block | Covers | Pages (Word) | structural% / raw% | Notes |
|---|---|---|---|---|---|
| parity2-coverpage | "Facet" cover (Cover Pages.dotx) | wpg shape group with custGeom freeform art, theme lumMod fills, two embedded images, title/subtitle/author SDTs | 3 | p1 92.7/8.3 · p2 97.6/14.1 · p3 1.9/0.7 | **Finding:** the decorative geometric band (wpg/custGeom group) does **not render** — SDT text + layout are correct but the art is absent and the title is a few px offset. Body page (p3) clean. |
| parity2-watermark | "CONFIDENTIAL" watermark (Watermarks.dotx) + hand-added page borders + line numbering | VML WordArt (`v:textpath` on t136) watermark in header, page borders, `w:lnNumType` line numbering, all in one section | 4 | p1 64.6/17.9 · p2 3.7/11.4 · p3 3.7/11.4 · p4 39.7/6.6 | **Finding:** the VML WordArt watermark and the line-number margin column both **do not render**. Page **borders render correctly**. |
| parity2-equations | Quadratic/Fourier/Binomial/Taylor (Equations.dotx) | real Word display equations (`m:oMathPara`, producer OMML) | 1 | p1 86.8/3.6 | **Finding:** `m:oMathPara` renders **left-aligned at inline size** instead of **centered display math** with enlarged operators/tall fractions. Content is present and structurally correct, just mis-sized/positioned. |

## Engine findings surfaced (documented, not fixed — `packages/` untouched)

No fixture crashed the engine and none produced a blank page; all 13 render and
all page counts match Word. Three genuine rendering gaps and several
vertical-metrics drifts were surfaced:

1. **Display equations (`m:oMathPara`)** render left-aligned at inline size, not
   centered as display math with display-mode operator/fraction sizing
   (`parity2-equations` p1; visible in `parity/out/parity2-equations-p1.png`).
2. **Cover-page decorative art** — the Facet cover's wpg freeform-geometry group
   with theme fills does not paint (text/SDTs render fine)
   (`parity2-coverpage` p1/p2).
3. **VML WordArt watermark + line numbering** — a `v:textpath` watermark in the
   header and `w:lnNumType` margin line numbers both render nothing; page
   borders in the same section are correct (`parity2-watermark` p1/p4).
4. **Drop-cap geometry** — framed drop-cap letter size/hang and body wrap-around
   differ from Word (`parity2-dropcap` p1/p2).
5. **Text-box wrap/position** — anchored wps text boxes and a rotated box are
   mis-placed and text wraps differently (`parity2-textboxes`).
6. **Vertical drift** — dense tables render with slightly shorter rows than Word
   and multi-section/landscape pages carry sub-line vertical offsets that
   accumulate; content and structure are correct (`parity2-nestedtables` p2,
   `parity2-sections` p2/p3/p5). These inflate structural% while raw% stays
   modest (~5-12%).

---

## Batch-1 and earlier fixtures (reference)

Coverage and Word page counts. Baselines for these live in
`parity/history.jsonl` / the generated `parity/out/report.html`; re-run
`node scripts/parity-compare.mjs <name>` to refresh a single one.

| Fixture | Covers | Pages (Word) | Generator |
|---|---|---|---|
| parity-text | baseline body text / wrapping | 1 | docx lib |
| parity-tables | basic table borders/widths | 1 | docx lib |
| parity-lists | basic bullet/number lists | 1 | docx lib |
| parity-firstpage | different first-page header/footer | 2 | docx lib |
| parity-headerfooter | headers/footers, page numbers | 2 | docx lib |
| parity-comments | comment anchors / balloons | 1 | docx lib |
| parity-dividers | horizontal rules / paragraph borders | 1 | docx lib |
| parity-pictures | inline images | 1 | docx lib |
| parity-wrapmodes | image wrap (square, dist offsets) | 1 | make-parity2-fixtures.py family |
| parity-revisions | tracked changes (ins/del) | 1 | make-parity2-fixtures.py |
| parity-math | inline OMML equation | 1 | make-parity2-fixtures.py |
| parity-math2 | n-ary, matrix, delimiters (OMML) | 1 | make-parity2-fixtures.py |
| parity-columns | continuous-section 2 columns | 1 | make-parity2-fixtures.py |
| parity-colbalance | balanced columns before continuous break | 1 | make-parity2-fixtures.py |
| parity-pageborders | page border box | 1 | make-parity2-fixtures.py |
| parity-rowsplit | table row taller than a page | 3 | make-parity2-fixtures.py |
| parity-hftemplates | 4 real built-in header templates (SDT/ptab/pct-table/wps shapes) | — | make-hftemplate-fixture.py |
| sample | multi-page body, headings, spacing | 4 | docx lib |
| benchmark | pagination / widow-orphan stress | 4 | docx lib |
| exact | metric calibration doc | — | docx lib |
| msa | sanitized real contract (tables, headings, 1.15 spacing) | 8 | sanitize-docx.py |
| chronology | sanitized real doc | 2 | sanitize-docx.py |
| pleading | sanitized legal pleading (VML line-number sidebar) | 7 | sanitize-docx.py |
| forsale | sanitized flyer (fixed-height cells, dashed tear-offs) | — | sanitize-docx.py |
| pickett | sanitized real doc | 1 | sanitize-docx.py |
| real | sanitized real doc | — | sanitize-docx.py |

## Operational notes for exporting (hard-won)

- Word must be **running and the session unlocked** — AppleEvents that need a
  dialog time out (-1712) and LaunchServices `open -g` won't deliver documents
  into a locked session.
- After a Word **force-quit**, the `open -g` LaunchServices route silently stops
  delivering documents (`exists document` stays false with no dialog). Use the
  AppleScript `open file name` route (`scripts/export-parity2.sh` does this).
  It can pop a one-time Grant-File-Access sheet on a brand-new directory.
- Two failure modes, both now caught by `scripts/validate-docx.py` before Word:
  Word either **rejects** a file outright ("experienced an error opening" —
  throws on `open`, then a modal wedges later AppleEvents -1708/-1712) or opens
  it **with a silent repair** ("corrupt / recovered" dialog). A repaired open is
  just as bad as a rejection: its PDF reflects Word's fixed-up doc, not the
  bytes on disk. Defects hit while building this batch, all now guarded:
  - **`footnotePr`/`endnotePr` separators in settings.xml** (declaring footnote
    id -1/0) with **no** footnotes.xml/endnotes.xml part — Word errors with a
    FOOTNOTE-related message. This was in the shared settings of all 10
    hand-built fixtures in the first cut and triggered errors across the batch;
    the separators now live only in the notes fixture, which ships the parts.
  - **Empty table cell** (`<w:tc>` with only `<w:tcPr>`, no `<w:p>`) — every
    cell needs at least one block-level child. Word rejects the whole file.
  - **A cell ending in a nested `</w:tbl>`** — a cell's last block must be a
    paragraph; append a trailing `<w:p/>` after any nested table.
  - **Out-of-order `w:tcPr` children** — `gridSpan`/`vMerge` placed after
    `tcBorders`/`shd` (CT_TcPr is an ordered sequence: `tcW, gridSpan, vMerge,
    tcBorders, shd, textDirection, vAlign`). Repaired-open corruption in
    nestedtables — re-exported after the fix.
  - **Duplicate / out-of-order `w:tblBorders`** — a second `tblBorders` (or one
    after `tblCellMar`) in a `tblPr`. Repaired-open corruption in nestedtables.
  - **Out-of-order `w:pPr` children** — `jc` before `spacing` (spacing precedes
    jc in CT_PPr). Repaired-open corruption in textboxes — re-exported.
- Always **verify the exported PDF** (page count, first text line) — AppleEvents
  can report -1712/-1728 even when the save succeeded.
