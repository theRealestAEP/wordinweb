{
  "name": "wordinweb",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source high-fidelity Word/.docx viewer and editor for the web",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @wordinweb/core -w wordinweb",
    "build:packages": "npm run build -w @wordinweb/core -w @wordinweb/collab -w @wordinweb/server -w wordinweb",
    "typecheck": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/collab/tsconfig.json && tsc --noEmit -p packages/server/tsconfig.json && tsc --noEmit -p packages/react/tsconfig.json && tsc --noEmit -p examples/anon-share/tsconfig.json",
    "test": "npm run test -w @wordinweb/core",
    "test:all": "vitest run --root packages/core && vitest run --root packages/collab && vitest run --root packages/server && vitest run --root packages/react",
    "demo": "node scripts/dev.mjs",
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "perf:report": "node scripts/perf-report.mjs run"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/ws": "^8.18.1",
    "jsdom": "^29.1.1",
    "tsup": "^8.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "ws": "^8.21.1"
  }
}
