# WordInWeb AI collaboration invitation

The user has invited you to edit a live DOCX document with WordInWeb.

Do not open this invitation in a browser. Run the exact command included in
the user's message. It passes the short invitation URL directly to the agent
client. The command has this form:

    npx -y --package='https://collab.word-in-web.com/wordinweb-agent.tgz?v=short-invite-4' wordinweb-agent connect '<short invitation URL>'

The connect command starts a detached local bridge and returns a `sessionId`.
Codex uses one dedicated resident document thread, so its context stays warm
between private messages. The bridge waits without model turns. Run each
document command as a new short process:

    npx -y --package='https://collab.word-in-web.com/wordinweb-agent.tgz?v=short-invite-4' wordinweb-agent session '<sessionId>' '{"command":"sync"}'

Read the `instructions` value from the ready result. Start with `sync`, then
use one compact `context` inspection for broad text tasks. Use detailed
`overview`, `read`, `object`, or `spatial` inspection only when needed. Treat
each private chat message as the next task. Inspect the relevant content before
every edit. If an edit returns `needs_sync`, sync and inspect the changed
content before you retry. After each task, reply through document chat and end
the agent turn. The bridge starts the next turn after the next private message.
Stay idle between turns. Send a `close` command when the inviter ends the task.

The agent starts in suggestion mode. The inviter can switch it to direct editing
from the private chat panel. Available session commands: `sync`, `capabilities`,
`inspect`, `edit`, `chat`, `wait`, and `close`.

This URL is a bearer credential. Do not print it, save it in a file, or share
it with another process.
